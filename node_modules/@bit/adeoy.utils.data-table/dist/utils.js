"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.resolveObjProp = exports.like = void 0;

var like = function like(str, search) {
  if (typeof search !== "string" || str === null) {
    return false;
  }

  search = search.replace(new RegExp("([\\.\\\\\\+\\*\\?\\[\\^\\]\\$\\(\\)\\{\\}\\=\\!\\<\\>\\|\\:\\-])", "g"), "\\$1");
  search = search.replace(/%/g, ".*").replace(/_/g, ".");
  return RegExp("^" + search + "$", "gi").test(str);
};

exports.like = like;

var resolveObjProp = function resolveObjProp(path, obj) {
  return path.split('.').reduce(function (prev, curr) {
    return prev ? prev[curr] : null;
  }, obj);
};

exports.resolveObjProp = resolveObjProp;

//# sourceMappingURL=utils.js.map